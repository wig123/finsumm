# V4 实验方案

**创建日期**：2026-03-30

---

## 实验 Round 1（当前，不补数据）

使用现有 2551 条偏好对（V1 1488 + T06 1063），T06 的 729 条合成图表无 source_facts。

| 实验 | 数据 | 方法 | margin 策略 | 状态 |
|------|------|------|-----------|------|
| DPO-T06 | T06 1063 | 标准 DPO | — | ✅ 0.738 |
| DPO-V1 | V1 1488 | 标准 DPO | — | ✅ 0.745 |
| DPO-Merged | V1+T06 2551 | 标准 DPO | — | ✅ **0.761** |
| **FCPO-Merged-v2** | V1+T06 2551 | FCPO | 有源=delta 加权, 无源=1.0 | 🔄 训练中 |

### FCPO-Merged-v2 margin 设计

```
有源数据 (832条, 33%): margin = delta / mean_delta (范围 0.51-3.74)
无源数据 (1719条, 67%): margin = 1.0 (标准 DPO)

Loss: L_i = -log σ(β × margin_i × Δlogits_i)
```

---

## 实验 Round 2（补 T06 source_facts）

给 T06 的 729 条合成图表建 source_mapping + 提取 source_facts，然后重新跑全流程。

### Step 1: 扩展 source_mapping

T06 的合成图表文件名遵循相同规则（syn_v2_XXX / syn_XXX / prog_XXX / syn_v4_XXX / syn_v4f_XXX），可以用 V2 的 `build_source_mapping.py` 逻辑直接映射。

```bash
# 从 t06_candidates_1280.json 中提取合成图表的图片名
# 建立 image → source_path 映射（复用 V2 的路径规则）
# 输出: source_mapping_t06.json
```

### Step 2: 提取 source_facts

```bash
# 对新的 729 条合成图表运行 extract_source_facts.py（已修复解析 bug）
# 输出: source_facts_t06.json
```

### Step 3: 重新评分 T06 的 729 条合成图表

```bash
# 用 score_candidates.py 只对这 729 条重新评分（现在有 source_facts 了）
# 其他样本保持不变
```

### Step 4: 重新构建偏好对

T06 的 729 条合成图表需要重新选对（因为评分变了），其余保持不变。

### Step 5: 重新训练

| 实验 | 预期变化 |
|------|---------|
| DPO-Merged-R2 | T06 合成图表选对更准确 → 可能提升 |
| FCPO-Merged-R2 | 有源数据从 832→~1560 条，FCPO 覆盖率翻倍 |

---

## 评估统一口径

所有实验使用相同推理环境：
- **服务器**: pku-246, 8×3090
- **脚本**: `l2_dpo_inference.py`
- **MAX_PIXELS**: 1280×32×32 (当前)
- **Prompt**: 简短版（中/英）
- **Judge**: `l2_judge_eval.py`, gemini-flash-lite, 有 GT reference

### Baseline（同口径）

| 模型 | L2 Score | 备注 |
|------|---------|------|
| SFT baseline | 0.744* | 需确认是否同口径 |
| LF-2 (原始 DPO) | ? | 需在 246 上重新推理 |

*SFT 0.744 来自 V3 项目的推理（pku 服务器, `sft_only_diag`），大概率同口径但需验证。

---

## 时间线

| 任务 | 预计用时 | 依赖 |
|------|---------|------|
| FCPO-Merged-v2 训练 | ~40min | 🔄 进行中 |
| FCPO-Merged-v2 推理+评估 | ~15min | 训练完成 |
| 建 T06 source_mapping | ~10min | 无 |
| 提取 T06 source_facts | ~5min | source_mapping |
| 重新评分 729 条 | ~30min | source_facts |
| 重新构建偏好对 | ~5min | 评分完成 |
| Round 2 DPO 训练+推理+评估 | ~60min | 偏好对 |
| Round 2 FCPO 训练+推理+评估 | ~60min | 偏好对 |
