# Highcharts 单独测试
batch_name: highcharts_test

output_base_dir: ./batch_output

execution:
  parallel: false
  max_workers: 1
  use_async: false

charts:
  - chart_type: line
    language: en-US
    theme: macro_policy
    library: highcharts
    is_frontend: true
